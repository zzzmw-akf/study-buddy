# 考研能量站 · 绝对可靠部署指南

## 为什么这个方案一定可以？

之前的方案（P2P连接、MQTT公网、自建中继、隧道穿透）全部失败的原因都一样：

> **手机 4G/5G 流量走的运营商网络，只允许出站访问标准端口（80/443），无法"穿透公网"找到你的电脑。**

这个方案把你的中继服务器**直接部署到云端**，拥有一**个真实的公网 IP**，手机怎么上网都能连上。相当于：

> 你租了个街边店铺（云服务器），朋友不管从哪个方向来，都能找到门牌号进来。

---

## 方案：腾讯云轻量应用服务器

**费用**：首月 28 元起（学生价更低），之后 68 元/月
**位置**：北京/上海/广州机房任选，国内访问低延迟
**配置**：2核2G内存，3M带宽，完全够用

---

## 第一步：购买服务器（3分钟）

1. 打开 [腾讯云轻量应用服务器](https://cloud.tencent.com/product/lighthouse)
2. 点击「立即选购」
3. 选择配置：
   - **地域**：选离你最近的（北京/上海/广州）
   - **镜像**：选 **Ubuntu 22.04** 或 **Debian 12**
   - **套餐**：最便宜的 2核2G 即可
4. 付款，等待创建完成（约1分钟）
5. 进入控制台 → 找到你的服务器 → 点「登录」

> 💡 **学生福利**：如果你是在校大学生，搜索「腾讯云校园计划」，有更优惠的学生套餐。

---

## 第二步：一键部署（2分钟）

在服务器的终端里，粘贴以下命令：

```bash
# 下载项目
git clone https://github.com/zzzmw-akf/study-buddy.git
cd study-buddy

# 一键部署
bash deploy.sh
```

脚本会自动完成：
- 更新系统
- 安装 Node.js 20
- 安装依赖
- 部署中继服务器
- 开放 80 端口
- 设置开机自启

部署成功后，会显示访问地址：

```
访问地址:  http://你的服务器IP/
```

---

## 第三步：额外配置（可选但推荐）

### 配置腾讯云防火墙

服务器的安全组可能默认没开 80 端口。去腾讯云控制台：

1. 进入轻量应用服务器 → 你的服务器 → 「防火墙」
2. 点击「添加规则」
3. 端口填 `80`，协议选 `TCP`，策略选「允许」
4. 保存

### 绑定域名（推荐）

直接用 IP 访问虽然能用，但有个域名更好记：

1. 买一个域名（腾讯云/阿里云都可以，几块钱一年）
2. DNS 解析添加 A 记录，指向你的服务器 IP
3. 访问 `http://你的域名/` 即可

---

## 使用方式

部署完成后，手机和电脑都打开：

```
http://你的服务器IP/
```

1. 输入房间号（如 `KX2024`）
2. 学生选「我是学生」，监督者选「我是监督者」
3. 自动连接成功！

**无论手机用 4G/5G/WiFi，电脑用宽带/有线/WiFi，都能连上。**

---

## 常见问题

### Q: 一定要买腾讯云吗？
可以用任何带公网 IP 的 Linux 服务器（阿里云、华为云、AWS、Azure 都行），`deploy.sh` 支持 Ubuntu/Debian/CentOS。

### Q: 能免费吗？
- 如果有学校的教育邮箱，可以去申请 GitHub Student Pack，里面包含 DigitalOcean 免费额度
- 腾讯云/阿里云经常有新用户免费试用活动
- 找个已有的服务器也行

### Q: 服务器关机了怎么办？
服务设置了开机自启（systemd），开机后自动运行，不用担心。

### Q: 需要一直开着吗？
需要。你不学习的时候可以关机，使用时再开机。腾讯云支持随时关机/开机，关机期间不收费。

### Q: 安全吗？
HTTP 明文传输，数据只在你们两人之间流转。如果担心安全，可以之后配置 HTTPS（用 Let's Encrypt 免费证书）。
