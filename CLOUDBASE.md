# CloudBase 配置指南（5 分钟搞定）

## 为什么选 CloudBase？

- **腾讯自己的云服务**，国内机房，标准 HTTPS 443 端口
- **微信/QQ 扫码登录**即可，不需要填任何注册信息
- **免费额度**：每月 5GB 存储、500 万次数据库读取，两个人用根本用不完
- **实时数据库**：原生的 `watch()` 监听，消息秒级同步

---

## 一、创建环境（1 分钟）

1. 打开 https://console.cloud.tencent.com/tcb
2. 用**微信或 QQ 扫码登录**
3. 点击「新建环境」
4. 环境名称随便填（如 `study-buddy`）
5. 计费方式选「**按量付费**」（有免费额度，不超额度不花钱）
6. 地域选「**上海**」（默认）
7. 点击「立即开通」

## 二、获取环境 ID（30 秒）

1. 环境创建好后，在左侧菜单点「环境设置」
2. 复制「**环境 ID**」（类似 `study-xxxx-123456`）
3. 打开项目中的 `public/config.js`，填入：

```javascript
var TCB_ENV_ID = 'study-xxxx-123456';  // 替换为你的环境 ID
```

## 三、设置数据库权限（1 分钟）

1. 左侧菜单 →「数据库」
2. 点击「新建集合」，名称填 `messages`，确定
3. 在 `messages` 集合卡片上，点击「权限设置」
4. 将权限改为「**所有用户可读写**」或「**仅创建者可读写，所有用户可读**」
5. 保存

> ⚠️ 权限不够的话会收不到对方消息！

## 四、确认匿名登录已开启（30 秒）

1. 左侧菜单 →「身份认证」→「登录方式」
2. 确保「**匿名登录**」是开启状态（默认就是开启的）
3. 如果没开，点一下开关就行

## 五、使用

配置完成后，直接用浏览器打开 `public/index.html`，或者部署到 GitHub Pages：

```
https://zzzmw-akf.github.io/study-buddy/
```

输入房间号，选择角色，开始用！

---

## 免费额度够用吗？

| 资源 | 免费额度/月 | 你们用量估算 |
|------|-----------|------------|
| 数据库读 | 500 万次 | ~5000 次/天 ≈ 15 万/月 |
| 数据库写 | 300 万次 | ~1000 次/天 ≈ 3 万/月 |
| 存储 | 5 GB | 几乎为 0（每条消息很小） |

完全够用，相当于白嫖。

## 故障排查

| 问题 | 解决 |
|------|------|
| 连不上 | 检查 config.js 中的环境 ID 是否正确 |
| 收不到对方消息 | 检查数据库权限是否设为"所有用户可读写" |
| 匿名登录失败 | 检查「身份认证 → 匿名登录」是否开启 |
| 浏览器提示跨域 | CloudBase 默认允许任何域名，不需要额外配置 |
